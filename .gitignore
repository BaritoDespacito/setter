# Python
__pycache__/
*.pyc
*.pyo
.python-version

# Generated route images (debug output from generate.py / drawClimb)
*_climb.png
flask_stuff/climbs/

# Node / frontend
node_modules/
setter_app/node_modules/
setter_app/dist/

# IDE / OS
.idea/
.DS_Store

# Local env
.env

# Training checkpoints: only kilter_setter_best.pt (weights-only "deploy" format) is
# committed. "latest"/"resume" checkpoints carry optimizer state (~3x larger, over
# GitHub's 100MB blob limit) and are meant to stay local for resuming training.
kilter_setter_latest.pt
kilter_setter_latest_resume.pt
kilter_setter_best_resume.pt
flask_stuff/kilter_setter_latest.pt
flask_stuff/kilter_setter_latest_resume.pt
flask_stuff/kilter_setter_best_resume.pt
